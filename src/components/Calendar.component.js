import styled from 'styled-components';

export function Calendar() {
  return <Container>CALENDAR.component</Container>;
}

const Container = styled.div`
  border: 5px dotted green;
  background-color: orange;
`;
